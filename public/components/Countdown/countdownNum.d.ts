export declare const CountdownNum: ({ start, paused, next, max, measure, }: {
    start?: number;
    paused: boolean;
    next?: (args?: any) => void;
    max?: number;
    measure?: string;
}) => any[];
