import { ReactNode } from 'react';
export declare const IconTitle: ({ title, icon, bubbleText, className, alt, children, pulse, }: {
    title?: string;
    icon?: string;
    bubbleText?: string;
    className?: string;
    alt?: string;
    children?: ReactNode | ReactNode[];
    pulse?: boolean;
}) => import("react/jsx-runtime").JSX.Element;
